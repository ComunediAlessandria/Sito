Incremento di performance (minor tempo di risposta e minor utilizzo di ram) in caso di richiesta di pagina richiesta in precedenza, quando extreme caching &egrave; disattivato o richiede una rigenerazione.

Modificato l'oggetto Importa Pagina al fine di implementare direttamente l'importazione dal sito corrente

Implementato il riconoscimento di Internet Explorer 7 nella gestione eccezioni CSS

Modificato il meccanismo di segnalazione delle violazioni di Validazione durante l'editing: <br />ora vengono mostrate all'atto del salvataggio della pagina, per evitare la comparsa di messaggi di avvertimento al semplice inserimento di nuovi oggetti non ancora compilati.

Inserita nuova validazione intestazioni delle tabelle

Inserite 2 nuove validazioni durante l'editing: controllo presenza titolo pagina e verifica annidamento titoli

Bug fix nella mappa del sito a profondit&agrave; variabile in siti multilingua.
Implementato il CTRL+U per il sottolineato nell'editing dei paragrafi.

Modifica alla gestione dei template di sito con supporto al concetto di include per utilizzare subtemplate comuni in più template di sito.

Modifiche all'editor visuale per un più facile utilizzo da parte di non vedenti attraverso Screen reader

Completato il pieno supporto a siti in lingue non occidentali attraverso UTF8 (N.B. richiede MySQL 4.1 e template in UTF8)

Modifica all'oggetto calendario con possibilità di personalizzazione grafica di ogni giorno della settimana (utile per colorare diversamente sabati e domeniche)