<?xml version='1.0' encoding='iso-8859-1' ?>
<bo id='AxesBC'>

	<!-- rule id='1' kind='id'		value='15'		bcpage='BLOB:ID=333'	pagination='CA' / -->

	<source
		kind='file'
		format='xml'
		name='BC.xml'
		path='{K:kClassificationPath}/'
		outertag='bc'
		itemtag='rule'
	/>

	<key name='id' />

	<!-- TBD: i nomi dei campi sono per adesso hardcoded in clsBOArray -->

	<field name='kind'				type='varchar'		len='10'		default='id' />
	<field name='value'				type='varchar'		len='250'		default='' />
	<field name='bcpage'			type='varchar'		len='100'		default='' />
	<field name='pagination'		type='varchar'		len='2'			default='NO' />
	<field name='sort'				type='varchar'		len='4'			default='0' />
	<field name='mdvalue'			type='varchar'		len='10'		default='' />

	<field name='startdate'			type='varchar'		len='10'		default='' />
	<field name='shownavigation'	type='int'							default='0' />

	<event id='onAfterSave' when='onAfterSave' type='php'><![CDATA[

			// in $pars

	]]></event>
</bo>